version: '3'

services:
  postgres:
    image: postgres:latest
    container_name: sims_container
    environment:
      POSTGRES_DB: sims
      POSTGRES_USER: root
      POSTGRES_PASSWORD: passwd240209
    ports:
      - "5432:5432"
